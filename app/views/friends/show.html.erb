<p style="color: green"><%= notice %></p>

<div>
  <div class="card">
    <div class="card-body">
  <%= render @friend %>
    </div>
  </div>

  <div class="pt-2">
    <%= link_to "Edit this friend", edit_friend_path(@friend), {class: "btn btn-primary d-inline p-2"} %>
    <%= link_to "Back to friends", friends_path, {class: "btn btn-primary d-inline p-2"} %>

    <%= link_to "Destroy this friend", @friend, method: :delete, class: 'btn btn-danger d-inline p-2' %>
  </div>
</div>